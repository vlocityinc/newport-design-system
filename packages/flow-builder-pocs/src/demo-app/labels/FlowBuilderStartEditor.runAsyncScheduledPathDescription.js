export default 'At some point after all of the automation in the transaction is successfully committed to the database, Flow Builder executes this path. This path can include asynchronous calls to external systems.';
