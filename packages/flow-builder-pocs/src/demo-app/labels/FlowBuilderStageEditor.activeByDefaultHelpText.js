export default 'When an interview starts, automatically add the stage to $Flow.ActiveStages. Default active stages are sorted in ascending order. In master flows, $Flow.CurrentStage is set to the default active stage that has the lowest order.';
