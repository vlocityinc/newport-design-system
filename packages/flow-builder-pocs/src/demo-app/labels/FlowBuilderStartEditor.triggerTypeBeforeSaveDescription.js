export default 'This flow can only update fields on the record that launches the flow—before the record is saved to the database. When you use an Assignment element to set the $Record global variable’s fields, Salesforce updates that record’s fields for you.';
