export default 'Because this flow runs <i>before</i> a record is saved, you can only update the record that triggered the flow to run. To update other records, configure the trigger to run the flow <i>after</i> the record is saved.';
