export default 'The flow can look up and update fields on any record before the record is deleted from the database. The flow can also perform actions, like send an email. To create, update or delete any records, you must use Create Records, Update Records or Delete Records elements. The $Record global variable contains the Salesforce record that launches the flow.';
