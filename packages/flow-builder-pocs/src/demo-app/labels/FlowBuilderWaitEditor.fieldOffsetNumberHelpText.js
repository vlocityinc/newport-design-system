export default 'Set the number of hours or days to offset the resume time from the base time, the value of which is in the specified field. For example, to resume the flow 3 days before the base time, enter "-3" as the offset number and "Days" as the offset unit.';
