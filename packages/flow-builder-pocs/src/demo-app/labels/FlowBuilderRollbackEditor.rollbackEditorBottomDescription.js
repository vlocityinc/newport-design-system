export default 'Pending record operations are committed to the database when their enclosing database transaction is committed.  Database transactions are committed immediately before the execution of screen elements, pause elements, and local actions, and new database transactions are opened immediately after the execution of screen elements, pause elements.  Additionally, some invocable actions may cause an open database transaction to be committed, and a new database transaction to be immediately opened.  When this element is executed, any pending record operations that were issued during this flow’s execution will be rolled back and not committed as part of the transaction. Limits will not be reset.';
