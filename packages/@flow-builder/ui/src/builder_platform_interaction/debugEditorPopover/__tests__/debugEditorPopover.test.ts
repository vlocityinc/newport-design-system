// Add web driver test instead of jest
// Something is not easily figured out in current builder jest framework, e.g. wire mock
describe('debugEditorPopover', () => {
    describe('Default test', () => {
        it('default test', () => {
            expect(true).toEqual(true);
        });
    });
});
