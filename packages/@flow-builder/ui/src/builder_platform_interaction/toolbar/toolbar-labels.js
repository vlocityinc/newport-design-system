import flowPropertiesTitle from '@label/FlowBuilderToolbar.flowPropertiesTitle';
import draftLabel from '@label/FlowBuilderToolbar.draftLabel';
import draftTitle from '@label/FlowBuilderToolbar.draftTitle';
import activeLabel from '@label/FlowBuilderToolbar.activeLabel';
import activeTitle from '@label/FlowBuilderToolbar.activeTitle';
import activeTooltip from '@label/FlowBuilderToolbar.activeTooltip';
import deactivatedLabel from '@label/FlowBuilderToolbar.deactivatedLabel';
import deactivatedTitle from '@label/FlowBuilderToolbar.deactivatedTitle';
import runTitle from '@label/FlowBuilderToolbar.runTitle';
import debugTitle from '@label/FlowBuilderToolbar.debugTitle';
import saveTitle from '@label/FlowBuilderToolbar.saveTitle';
import errorPopOverHeader from '@label/FlowBuilderToolbar.errorPopOverHeader';

export const LABELS = {
    flowPropertiesTitle,
    draftLabel,
    draftTitle,
    activeLabel,
    activeTitle,
    activeTooltip,
    deactivatedLabel,
    deactivatedTitle,
    runTitle,
    debugTitle,
    saveTitle,
    errorPopOverHeader
};